# 多变量时间序列预测设计思路

本方案参考 iTransformer 实现多变量长期预测任务。任务输入为 96 个历史时间步、100 个变量，输出为 96、192、336、720 四种未来长度的 100 变量预测结果。核心思路是保留 iTransformer 的倒置变量建模结构，把 100 个变量作为 token，在变量维度上做注意力建模，再通过线性投影直接输出指定预测长度。

数据处理方面，训练集 `train/train.csv` 为无时间戳的连续多变量序列，因此不使用原参考代码中依赖 `date` 列的 `Dataset_Custom`，而是新增 `TSForecastDataset`。该数据集按时间顺序划分训练和验证区间，默认前 80% 用于训练，后 20% 用于验证；训练样本由滑动窗口构造，输入窗口长度固定为 96，标签窗口长度为对应预测步长。标准化参数只在训练区间拟合，验证集和测试集复用同一组均值和标准差，避免验证信息泄漏。由于数据没有真实时间戳，模型输入中的时间特征传入 `None`，直接使用 iTransformer 的变量倒置嵌入。

模型训练采用四个独立模型，分别对应 `pred_len=96,192,336,720`。这样每个模型都直接优化自己的预测目标，避免使用单个 720 步模型切片造成短期预测目标不匹配。训练目标为 MSE，验证时同样计算 MSE，并保存验证集 MSE 最低的 checkpoint。默认超参数面向 A6000 显卡设置为 `batch_size=128`、`d_model=768`、`e_layers=3`、`n_heads=12`、`d_ff=3072`、`dropout=0.1`、`learning_rate=1e-4`，并默认开启 AMP 混合精度；若显存不足可以将 `d_model` 降为 256 或减小 batch size。

推理阶段读取测试张量 `250 x 96 x 100`，对每个预测长度加载对应 checkpoint 和 scaler，先进行标准化，再输入模型，最后反标准化回原始数值空间。输出文件严格命名为 `pred_96.npy`、`pred_192.npy`、`pred_336.npy`、`pred_720.npy`，形状分别为 `250 x 96 x 100`、`250 x 192 x 100`、`250 x 336 x 100`、`250 x 720 x 100`。评价指标与 `test_demo` 保持一致，对四个预测长度分别计算 MSE，最终取四个 MSE 的平均值。
